@if(Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOut", "Accounts", new { Area = "" }, FormMethod.Post,
        new { id = "logoutForm" }))
    {
        <ul class="nav navbar-nav nav-flex-icons ml-auto ">
            <li>
                <a href="javascript:document.getElementById('logoutForm').submit();">LogOut</a>
            </li>
        </ul>
    }
}
else
{
        <ul class="nav navbar-nav nav-flex-icons ml-auto">
            @*<li>@Html.ActionLink("Register", "Register", "Accounts", new { Area = "" }, null)</li>
        <li>@Html.ActionLink("Login", "Login", "Accounts", new { Area = "" }, null)</li>*@

            <li class=""><a class="nav-link ml-auto" href="/Authorization"><span class="btn btn-primary btn-rounded btn-sm"> LogIn <i class="fas fa-key"></i> </span></a></li>
            <li class=""><a class="nav-link ml-auto" href="/Authorization"><span class="btn btn-deep-orange btn-rounded btn-sm"> Register <i class="fas fa-key"></i> </span></a></li>
        </ul>
}
